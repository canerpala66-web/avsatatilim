"use client";
import { useMemo,useState } from "react"; import { blogPosts,categories } from "@/data/blog"; import BlogCard from "./BlogCard";
export default function BlogExplorer(){const [cat,setCat]=useState("Tümü");const [q,setQ]=useState("");const posts=useMemo(()=>blogPosts.filter(p=>(cat==="Tümü"||p.category===cat)&&(`${p.title} ${p.excerpt} ${p.keywords.join(" ")}`.toLocaleLowerCase("tr").includes(q.toLocaleLowerCase("tr")))),[cat,q]);return <><div className="glass !shadow-[0_18px_50px_#073b4c12] rounded-[28px] p-4 mb-9 flex flex-col gap-4"><div className="flex flex-col md:flex-row gap-3 justify-between"><div className="flex gap-2 overflow-x-auto pb-1">{categories.map(c=><button key={c} onClick={()=>setCat(c)} className={`whitespace-nowrap rounded-full px-4 py-2.5 text-xs font-bold transition ${cat===c?"bg-[#073b4c] text-white shadow-lg":"bg-white/75 text-[#42636b] hover:bg-[#e5f5f2]"}`}>{c}</button>)}</div><label className="relative shrink-0"><span className="absolute left-4 top-1/2 -translate-y-1/2 text-[#6b888e]">⌕</span><input value={q} onChange={e=>setQ(e.target.value)} className="h-11 rounded-full border border-[#cbdedd] bg-white pl-10 pr-4 text-sm min-w-[240px] outline-none focus:ring-2 focus:ring-[#73cfc7]" placeholder="Rehberlerde ara..." aria-label="Blog yazılarında ara"/></label></div></div><p className="text-xs uppercase tracking-widest font-bold text-[#74878c] mb-5">{posts.length} rehber • seçilmiş Avşa notları</p><div className="grid md:grid-cols-2 lg:grid-cols-3 gap-5">{posts.map(p=><BlogCard post={p} key={p.slug}/>)}</div></>}
