import Breadcrumbs from "./Breadcrumbs";
export default function PageHero({eyebrow,title,description,crumb}:{eyebrow:string;title:string;description:string;crumb:string}){const variant=title.length%3;return <section className={`page-hero page-hero-${variant}`}><div className="container py-16 md:py-28 relative z-[1]"><div className="absolute right-[5%] bottom-8 hidden md:grid w-28 h-28 place-items-center rounded-[38%_62%_55%_45%] border border-[#087f8c22] bg-white/45 backdrop-blur text-4xl rotate-6">{variant===0?"≈":variant===1?"☀":"✦"}</div><Breadcrumbs items={[{label:crumb}]}/><div className="trust-pill !text-[#08717c] !bg-white/55 !border-white mb-5">{eyebrow}</div><h1 className="display max-w-4xl !text-[clamp(2.6rem,5.4vw,5rem)]">{title}</h1><p className="lead max-w-2xl mt-6 text-[1.08rem]">{description}</p></div></section>}
