import Breadcrumbs from "./Breadcrumbs";
export default function PageHero({eyebrow,title,description,crumb}:{eyebrow:string;title:string;description:string;crumb:string}){const variant=title.length%3;return <section className={`page-hero page-hero-${variant}`}><div className="page-hero-orbit"/><div className="container relative z-[1]"><div className="page-hero-code"><span>AVŞA</span><i>0{variant+1}</i><span>REHBER</span></div><Breadcrumbs items={[{label:crumb}]}/><div className="trust-pill !text-[#08717c] !bg-white/60 !border-white mb-5">{eyebrow}</div><h1 className="display !text-[clamp(2.9rem,5.7vw,5.7rem)]">{title}</h1><div className="page-hero-bottom"><p className="lead max-w-2xl text-[1.08rem]">{description}</p><span className="page-hero-mark" aria-hidden="true">{variant===0?"≈":variant===1?"☀":"✦"}</span></div></div></section>}
