export default function SectionHeading({eyebrow,title,description,center=false}:{eyebrow:string;title:string;description?:string;center?:boolean}){return <div className={`section-heading ${center?"text-center mx-auto section-heading-center":""} max-w-3xl mb-10`}><div className="eyebrow mb-3">{eyebrow}</div><h2 className="title">{title}</h2>{description&&<p className="lead mt-4">{description}</p>}<span className="heading-signature" aria-hidden="true"><i/><b>≈</b></span></div>}
