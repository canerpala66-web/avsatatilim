import Link from "next/link"; import { Icon } from "./Icon"; import SiteImage from "./SiteImage";
export default function RoomCard({room}:{room:{name:string;capacity:string;balcony:string;description:string;image:string;alt:string}}){return <article className="card room-card overflow-hidden group"><SiteImage src={room.image} alt={room.alt} sizes="(max-width: 640px) 100vw, (max-width: 1024px) 50vw, 25vw" className="room-image aspect-[4/3]"><span className="room-capacity">{room.capacity}</span><span className="room-index" aria-hidden="true">ODA</span></SiteImage><div className="p-6"><h3 className="text-2xl text-[#073b4c]">{room.name}</h3><p className="text-sm leading-6 text-[#6a7f85] my-3">{room.description}</p><div className="room-features">{["Klima","Özel banyo","Wi-Fi",room.balcony].map(x=><span key={x}><Icon name="check" size={12}/>{x}</span>)}</div><Link className="btn btn-outline w-full !min-h-11" href={`/rezervasyon?oda=${encodeURIComponent(room.name)}`}>Müsaitlik Sor</Link></div></article>}
