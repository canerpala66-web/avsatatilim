import Link from "next/link"; import type { BlogPost } from "@/data/blog"; import { blogCategoryImages } from "@/data/site"; import { Icon } from "./Icon"; import SiteImage from "./SiteImage";
export default function BlogCard({post,featured=false}:{post:BlogPost;featured?:boolean}){const image=blogCategoryImages[post.category]||blogCategoryImages["Avşa Rehberi"];return <article className={`card magazine-card overflow-hidden group h-full ${featured?"magazine-card-featured md:grid md:grid-cols-[1.08fr_.92fr] md:items-center":""}`}><SiteImage src={image.src} alt={`${post.title}: ${image.alt}`} priority={featured} sizes={featured?"(max-width: 768px) 100vw, 52vw":"(max-width: 768px) 100vw, 33vw"} className={`magazine-image w-full overflow-hidden ${featured?"aspect-[16/10] md:aspect-[4/3]":"aspect-[16/10]"}`}><div className="absolute inset-0 z-[1] bg-gradient-to-t from-[#012c3d99] via-transparent to-transparent"/><span className="magazine-category">{post.category}</span><span className="magazine-arrow"><Icon name="arrow" size={17}/></span></SiteImage><div className={`${featured?"p-7 md:p-10 flex flex-col justify-center":"p-6"}`}><div className="magazine-meta"><span>{post.readingTime} okuma</span><i/><span>{new Date(post.date).getFullYear()}</span></div><h3 className={`${featured?"text-3xl md:text-4xl":"text-[23px]"}`}>{post.title}</h3><p className={`${featured?"text-base":"text-sm"}`}>{post.excerpt}</p><Link href={`/blog/${post.slug}`} className="magazine-link">Rehberi oku <Icon name="arrow" size={17}/></Link></div></article>}
