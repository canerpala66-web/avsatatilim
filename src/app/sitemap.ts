import type { MetadataRoute } from "next"; import { blogPosts } from "@/data/blog"; import { site } from "@/data/site";
export default function sitemap():MetadataRoute.Sitemap{const pages=["","avsa-otelleri","atas-aqua-otel","odalar","havuz-aquapark","avsa-rehberi","avsa-ulasim","blog","iletisim","rezervasyon"].map(x=>({url:`${site.siteUrl}/${x}`,lastModified:new Date(),changeFrequency:"weekly" as const,priority:x===""?1:.8}));return [...pages,...blogPosts.map(p=>({url:`${site.siteUrl}/blog/${p.slug}`,lastModified:new Date(p.date),changeFrequency:"monthly" as const,priority:.7}))]}
